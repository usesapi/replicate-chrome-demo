import React from "react";
import ReactDOM from "react-dom";

const Options = () => {
  return <div>Options for your ext</div>;
};

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);
